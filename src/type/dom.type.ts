export type DomType = {
    showName: string,
    allowDomains: Array<string>,
    id: string,
    tags: Array<string>
    elements: Array<any>
}

export type DomeEditor = {}
export type DomEditorElement = {}